# ID Tokens & OpenID Connect

ID Token contains user identity info, used in OIDC flows. `aud` is the client ID. Always verify `nonce` and `iat/exp`.

> 📘 Next: [Token Binding & OAuth 2.0 mTLS](04-token-binding-and-oauth2-mtls.md)
